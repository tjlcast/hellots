#!/usr/bin/env node

require('../dist/index');
// 使用 require 函数加载编译后的模块
// const { findLatestFiles } = require('../dist/index');

// // 获取命令行参数
// const directory = process.argv[2];
// const count = process.argv[3];

// // 调用函数并输出结果
// const latestFile = findLatestFiles(directory, count);
// if (latestFile) {
//   console.log(`The latest changed file is: ${latestFile}`);
// } else {
//   console.log(`No files found in the directory: ${directory}`);
// }
